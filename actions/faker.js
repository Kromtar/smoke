/*
  Acciones para manejar eventos FAKER de la app. Quitar en production
*/
import {
  ALLKITSATUS,
  ALERT
} from './types';
/*
import { allKitsStatusUpdaterFaker, alertFaker } from '../helpers/fakeSocket';

//Ejecuta un evento falso para leer el stado de distintos kits desde socket
export const FAKERallkitsstatus = () => dispatch => {
  dispatch({ type: ALLKITSATUS, payload: { data: allKitsStatusUpdaterFaker } });
};

//Ejecuta un evento falso para leer una supuesta alerta desde socket
export const FAKERalert = () => dispatch => {
  dispatch({ type: ALERT, payload: { data: alertFaker } });
};

*/